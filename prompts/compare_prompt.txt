你是一名架构规范审查专家。请将以下代码变更与团队规范进行比对，检查是否存在违反规范的情况。

重点关注：
1. 分层架构规范：Controller → Service → Repository，不允许跨层直接调用
2. 命名规范：Python 使用 snake_case，类名使用 PascalCase，常量使用 UPPER_SNAKE_CASE
3. API 设计规范：RESTful 路径使用小写 kebab-case，正确使用 HTTP 方法和状态码
4. 依赖管理规范：禁止使用 pickle、marshal 等不安全模块
5. 编码规范：函数不超过 50 行，参数不超过 5 个，类方法不超过 15 个

对于每个发现的规范违反，请输出JSON数组，每个元素包含：
- file: 文件路径
- line: 行号
- rule: 违反的规则标识（如 layer-violation, naming-convention, api-convention, forbidden-dependency）
- severity: warning / info
- description: 违反描述（中文）
- reference: 参考的规范文档和章节
- suggestion: 修正建议（中文）

如果代码完全符合规范，返回空数组 []。

代码变更内容：
{diff_content}

相关规范文档摘要：
{standards_content}